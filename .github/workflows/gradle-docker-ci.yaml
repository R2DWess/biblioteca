name: Java CI/CD with Gradle and Docker

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: biblioteca
          MYSQL_USER: user
          MYSQL_PASSWORD: password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --host=localhost --user=user --password=password"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      # ✅ JDK 17 (Melhor compatibilidade e performance)
      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # ⚡ Configura Gradle e Cache (Melhora performance do CI)
      - name: Configurar Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Cache do Gradle
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      # ✅ Aguarda MySQL estar pronto antes de rodar testes
      - name: Verificar conexão com MySQL
        run: |
          sudo apt-get install -y mysql-client
          echo "Aguardando MySQL estar pronto..."
          until mysql --host=127.0.0.1 --user=user --password=password -e "SELECT 1"; do
            echo "MySQL ainda não está pronto... tentando novamente em 5s"
            sleep 5
          done
          echo "MySQL está pronto!"

      - name: Conceder permissão de execução ao Gradle Wrapper
        run: chmod +x gradlew

      - name: Configurar variáveis de ambiente para o Gradle
        run: |
          echo "SPRING_PROFILES_ACTIVE=docker" >> $GITHUB_ENV

      # 🔍 Exibir logs do MySQL para debugging antes de rodar os testes
      - name: Exibir logs do MySQL
        run: docker logs $(docker ps -q --filter ancestor=mysql:8)

      - name: Build e Testes com Gradle
        run: ./gradlew build --no-daemon

  docker:

    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Login no Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build da Imagem Docker
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/biblioteca:latest .

      - name: Push da Imagem para o Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/biblioteca:latest

  dependency-submission:

    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gerar e submeter o gráfico de dependências
        uses: gradle/actions/dependency-submission@v4
